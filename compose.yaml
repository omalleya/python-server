services:
  web:
    build: .
    ports:
      - "8000:8000"
    develop:
      watch:
        - action: sync
          path: .
          target: /app
    # command: fastapi dev main.py --host 0.0.0.0 --reload
  redis:
    image: "redis:alpine"
